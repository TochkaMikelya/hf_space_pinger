name: Keep HF Space Alive

on:
  schedule:
    - cron: '0 */12 * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: curl https://huggingface.co/spaces/TochkaMikelya/article_classifier_abylay