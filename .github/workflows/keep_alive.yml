name: Keep HF Space Alive

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: curl https://tochkamikelya-article-classifier-abylay.hf.space